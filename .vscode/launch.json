{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug server (no air)",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/server/main.go",
            "cwd": "${workspaceFolder}",
            "env": {
                "PORT": "8081"
            }
        }
    ]
}